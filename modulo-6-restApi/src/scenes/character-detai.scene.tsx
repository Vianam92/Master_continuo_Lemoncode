import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterContainer } from 'pods/detail-character';

export const CharacterDetailScene = () => (
  <AppLayout>
    <CharacterContainer />
  </AppLayout>
);
